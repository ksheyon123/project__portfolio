export interface NavProps {
  order: number;
  key: string;
  name: string;
  route: string;
  category: number;
}

export interface CategoryProps {
  id: number;
  name: string;
  category: number;
}